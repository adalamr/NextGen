import axios from 'axios'; export class JiraConnector { constructor(private config: { baseUrl: string; email: string; apiToken: string }) {} async getIssues(projectKey: string, jql?: string) { const res = await axios.get(${this.config.baseUrl}/rest/api/3/search, { auth: { username: this.config.email, password: this.config.apiToken }, params: { jql: jql || project =  } }); return res.data.issues; } }
