# ============================================================
# AI TEST PLATFORM - ENVIRONMENT VARIABLES
# Rename this file to .env and fill in the values
# ============================================================

# --- App ---
NODE_ENV=development
APP_NAME=AI Test Platform
APP_PORT=4000
APP_URL=http://localhost:4000
WEB_URL=http://localhost:3000

# --- Database (PostgreSQL + pgvector) ---
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ai_test_platform
DB_USER=postgres
DB_PASSWORD=postgres
DB_SSL=false
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ai_test_platform

# --- Redis (BullMQ + Cache) ---
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_URL=redis://localhost:6379

# --- JWT Auth ---
JWT_SECRET=your_super_secret_jwt_key_change_in_production
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your_refresh_secret_key_change_in_production
JWT_REFRESH_EXPIRES_IN=30d

# --- AWS Bedrock LLM (User configurable per project) ---
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
BEDROCK_API_ENDPOINT=https://bedrock-runtime.us-east-1.amazonaws.com
BEDROCK_DEFAULT_MODEL=anthropic.claude-3-5-sonnet-20241022-v2:0

# --- File Storage ---
STORAGE_PROVIDER=local
LOCAL_STORAGE_PATH=./uploads
AWS_S3_BUCKET=ai-test-platform-files
AWS_S3_REGION=us-east-1
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=ai-test-platform

# --- CI/CD Integrations ---
AZURE_DEVOPS_ORG_URL=https://dev.azure.com/your-org
AZURE_DEVOPS_PAT=your_personal_access_token
JENKINS_URL=http://localhost:8080
JENKINS_USER=admin
JENKINS_TOKEN=your_jenkins_api_token

# --- External Integrations ---
JIRA_BASE_URL=https://your-org.atlassian.net
JIRA_EMAIL=your@email.com
JIRA_API_TOKEN=your_jira_api_token
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token
SLACK_SIGNING_SECRET=your_slack_signing_secret
WEBHOOK_SECRET=your_webhook_signing_secret

# --- Observability ---
LOG_LEVEL=debug
LOG_FORMAT=pretty
METRICS_ENABLED=false
METRICS_PORT=9090
