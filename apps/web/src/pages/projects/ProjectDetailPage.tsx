import React from 'react'; import { useParams } from 'react-router-dom'; export default function ProjectDetailPage() { const { id } = useParams(); return <div><h1 className='text-2xl font-bold mb-4'>Project {id}</h1></div>; }
